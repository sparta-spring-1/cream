spring:
# datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/testdb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    username: testuser
#    password: testpassword
  h2:
    console:
      enabled: true  # H2 Console을 사용할지 여부 (H2 Console은 H2 Database를 UI로 제공해주는 기능)
      path: /h2-console  # H2 Console의 Path
  # Database Setting Info (Database를 H2로 사용하기 위해 H2연결 정보 입력)
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;MODE=MYSQL
    username: sa
    password:

  data:
    redis:
      host: localhost
      port: 6379
      password: test-password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: test-secret-key-at-least-256-bits-long-for-test-purposes
  issuer: test-issuer
  access-exp-sec: 900
  refresh-exp-sec: 1209600

portone:
  api:
    base-url: https://api.portone.io
    store-id: test-store-id
    secret: test-secret
    channel-key: test-channel-key
